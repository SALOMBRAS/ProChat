import type { ContactListQuery, ContactListResponse, CreateSessionRequest, CreateTemplateRequest, SessionQr, SessionSummary, TemplateListResponse, UpdateTemplateRequest, WhatsAppSession } from '@chatpro/contracts';
import type { RequestContext } from '@chatpro/contracts';
export interface SessionServicePort { list(context: RequestContext): Promise<SessionSummary[]>; create(context: RequestContext, input: CreateSessionRequest): Promise<WhatsAppSession>; get(context: RequestContext, sessionId: string): Promise<WhatsAppSession>; qr(context: RequestContext, sessionId: string): Promise<SessionQr>; connect(context: RequestContext, sessionId: string, forceQrRefresh: boolean): Promise<void>; disconnect(context: RequestContext, sessionId: string): Promise<void>; logout(context: RequestContext, sessionId: string): Promise<void>; remove(context: RequestContext, sessionId: string): Promise<void>; }
export interface ContactServicePort { list(context: RequestContext, query: ContactListQuery): Promise<ContactListResponse>; }
export interface TemplateServicePort { list(context: RequestContext): Promise<TemplateListResponse>; create(context: RequestContext, input: CreateTemplateRequest): Promise<void>; update(context: RequestContext, id: string, input: UpdateTemplateRequest): Promise<void>; remove(context: RequestContext, id: string): Promise<void>; }
